name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup runme
      uses: ./.github/actions/runme

    - name: Setup node
      uses: ./.github/actions/node

    - run: runme run install-deps
    - run: runme run lint
    - run: runme run unit:test
